#A function that can acheive: For a user-selected period, retrieve all accidents that contains a keyword (user entered) in the DCA_CODE, e.g. animal, ped.
from file_loader import fileloader
data = fileloader()
